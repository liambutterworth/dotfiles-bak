[user]
    name = Liam Butterworth
    email = account@liambutterworth.me
[core]
    editor = vim
[credential]
    helper = cache
[alias]
    root = !cd $(git rev-parse --show-toplevel) && echo $(pwd)
