# Install

```
    $ brew install zsh tmux vim git ctags
    $ brew cask tap homebrew/cast-fonts
    $ brew install iterm2 font-source-code-pro
    $ git clone https://github.com/liambutterworth/dotfiles ~/.dotfiles
    $ ~/.dotfiles/bin/install
```
